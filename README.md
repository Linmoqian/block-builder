# 积木库

每个积木对应一段代码。
代码会与 `TmpSrc/sample.py` 实时同步。

# 快速开始

```bash
git clone https://github.com/Linmoqian/block-builder
cd block-builder
conda env create -f environment.yml
npm install
npm run dev
python server.py
```